import 'package:flutter/material.dart';

class DM extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text("Direct"),
      ),
      body: Center(
        child: Text('DM not yet Ready!'),
      ),
    );
  }
}
